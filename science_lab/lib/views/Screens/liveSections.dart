import 'package:flutter/material.dart';

class LiveSections extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:  Center(child: Text("Live")),
    );
  }
}
